<Window x:Class="ConversorDrawind.UI.Wpf.Layers.LayerBaseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleLayerBase}"
        Width="310"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Loaded="WindowLoaded">
    <Border Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="64" />
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="BaseLayerComboBox"
                      Grid.Row="0"
                      Grid.Column="0"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      Margin="0,0,8,12" />
            <Button Grid.Row="0"
                    Grid.Column="1"
                    Content="{x:Static local:Localization.ButtonEllipsis}"
                    Margin="0,0,0,12"
                    Click="SearchButtonClick" />

            <StackPanel Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{x:Static local:Localization.ButtonContinue}"
                        IsDefault="True"
                        MinWidth="96"
                        Margin="0,0,8,0"
                        Click="ContinueButtonClick" />
                <Button Content="{x:Static local:Localization.ButtonCancel}"
                        IsCancel="True"
                        MinWidth="96"
                        Click="CancelButtonClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
