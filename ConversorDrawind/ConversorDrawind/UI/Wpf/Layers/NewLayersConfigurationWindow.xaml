<Window x:Class="ConversorDrawind.UI.Wpf.Layers.NewLayersConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleLayerConfiguration}"
        Width="714"
        Height="403"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="{x:Static local:Localization.ButtonLoad}" Width="168" Height="30" Click="LoadClick" />
            <Button Content="{x:Static local:Localization.ButtonClearAll}" Width="168" Height="30" Margin="6,0,0,0" Click="ClearAllClick" />
            <Button Content="{x:Static local:Localization.ButtonAdd}" Width="168" Height="30" Margin="6,0,0,0" Click="AddRowClick" />
            <Button Content="{x:Static local:Localization.ButtonDelete}" Width="168" Height="30" Margin="6,0,0,0" Click="DeleteRowClick" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="{x:Static local:Localization.ButtonContinue}" Width="168" Height="30" IsDefault="True" Click="ContinueClick" />
            <Button Content="{x:Static local:Localization.ButtonCancel}" Width="168" Height="30" Margin="6,0,0,0" IsCancel="True" Click="CancelClick" />
        </StackPanel>

        <DataGrid x:Name="LayersGrid"
                  Margin="0,6,0,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding Rows}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="LayersGridMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerName}" Binding="{Binding Layer}" Width="220" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerColor}" Binding="{Binding Color}" Width="220" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerLine}" Binding="{Binding Line}" Width="220" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
