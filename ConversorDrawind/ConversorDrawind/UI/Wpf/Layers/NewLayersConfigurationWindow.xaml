<Window x:Class="ConversorDrawind.UI.Wpf.Layers.NewLayersConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Novos Layers"
        Width="714"
        Height="403"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="Carregar" Width="168" Height="30" Click="LoadClick" />
            <Button Content="Limpar tudo" Width="168" Height="30" Margin="6,0,0,0" Click="ClearAllClick" />
            <Button Content="Incluir linha" Width="168" Height="30" Margin="6,0,0,0" Click="AddRowClick" />
            <Button Content="Excluir linha" Width="168" Height="30" Margin="6,0,0,0" Click="DeleteRowClick" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="Continuar" Width="168" Height="30" IsDefault="True" Click="ContinueClick" />
            <Button Content="Cancelar" Width="168" Height="30" Margin="6,0,0,0" IsCancel="True" Click="CancelClick" />
        </StackPanel>

        <DataGrid x:Name="LayersGrid"
                  Margin="0,6,0,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding Rows}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="LayersGridMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome do Layer" Binding="{Binding Layer}" Width="220" />
                <DataGridTextColumn Header="Cor do Layer" Binding="{Binding Color}" Width="220" />
                <DataGridTextColumn Header="Linha do Layer" Binding="{Binding Line}" Width="220" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
