<Window x:Class="ConversorDrawind.UI.Wpf.Layers.LayerLineTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Estilo de linha"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ComboBox x:Name="LineTypeComboBox"
                      Grid.Row="0"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      Margin="0,0,0,12" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Continuar" IsDefault="True" MinWidth="96" Margin="0,0,8,0" Click="ContinueButtonClick" />
                <Button Content="Cancelar" IsCancel="True" MinWidth="96" Click="CancelButtonClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
