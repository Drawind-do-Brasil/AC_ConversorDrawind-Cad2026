<UserControl x:Class="ConversorDrawind.UI.Wpf.Layers.NewLayersConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ConversorDrawind">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="{x:Static local:Localization.ButtonLoad}"  Padding="10,0" Click="LoadClick" />
            <Separator Visibility="Hidden" Margin="2.5"/>
            <Button Content="{x:Static local:Localization.ButtonClearAll}"  Padding="10,0"  Click="ClearAllClick" />
            <Separator Visibility="Hidden" Margin="2.5"/>
            <Button Content="{x:Static local:Localization.ButtonAdd}"  Padding="10,0"  Click="AddRowClick" />
            <Separator Visibility="Hidden" Margin="2.5"/>
            <Button Content="{x:Static local:Localization.ButtonDelete}"   Padding="10,0"  Click="DeleteRowClick" />
        </StackPanel>

        <DataGrid x:Name="LayersGrid"
                  Margin="0,6,0,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding Rows}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="LayersGridMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerName}" Binding="{Binding Layer}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerColor}" Binding="{Binding Color}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelLayerLine}" Binding="{Binding Line}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
