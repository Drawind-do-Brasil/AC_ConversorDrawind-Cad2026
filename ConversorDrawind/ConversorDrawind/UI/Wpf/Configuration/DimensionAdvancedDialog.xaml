<Window x:Class="ConversorDrawind.UI.Wpf.Configuration.DimensionAdvancedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleAdvancedDimensionConfiguration}"
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="128" />
            </Grid.ColumnDefinitions>

            <CheckBox x:Name="FixArrowCheckBox"
                      Grid.Row="0"
                      Grid.Column="0"
                      Grid.ColumnSpan="2"
                      Content="{x:Static local:Localization.LabelFixDimensionArrows}"
                      Margin="0,0,0,8" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="{x:Static local:Localization.LabelArrowCollisionTarget}"
                       VerticalAlignment="Center"
                       Margin="0,0,8,4" />
            <ComboBox x:Name="ArrowTypeComboBox"
                      Grid.Row="1"
                      Grid.Column="1"
                      IsEditable="False"
                      Margin="0,0,0,4" />

            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="{x:Static local:Localization.LabelArrowTransitionDistance}"
                       VerticalAlignment="Center"
                       Margin="0,0,8,12" />
            <TextBox x:Name="DistanceTextBox"
                     Grid.Row="2"
                     Grid.Column="1"
                     Margin="0,0,0,12"
                     PreviewTextInput="DistanceTextBoxPreviewTextInput" />

            <StackPanel Grid.Row="3"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{x:Static local:Localization.ButtonContinue}"
                        IsDefault="True"
                        Width="101"
                        Margin="0,0,6,0"
                        Click="ContinueButtonClick" />
                <Button Content="{x:Static local:Localization.ButtonCancel}"
                        IsCancel="True"
                        Width="101"
                        Click="CancelButtonClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
