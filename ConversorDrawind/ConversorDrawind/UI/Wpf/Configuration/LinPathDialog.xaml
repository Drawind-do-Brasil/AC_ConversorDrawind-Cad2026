<Window x:Class="ConversorDrawind.UI.Wpf.Configuration.LinPathDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleAttention}"
        Width="620"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="O caminho especificado para o arquivo (&quot;acad.lin&quot;) do AutoCAD 2026 não existe. Por favor, especifique o arquivo correto."
                   TextWrapping="Wrap" />
        <Grid Grid.Row="1" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="105" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="{x:Static local:Localization.ButtonBrowse}"
                    Height="24"
                    Click="Browse_Click" />
            <TextBox x:Name="PathTextBox"
                     Grid.Column="1"
                     Margin="8,0,0,0"
                     Height="24" />
        </Grid>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Content="{x:Static local:Localization.ButtonContinue}"
                    Width="101"
                    Height="24"
                    IsDefault="True"
                    Click="Continue_Click" />
            <Button Content="{x:Static local:Localization.ButtonCancel}"
                    Width="101"
                    Height="24"
                    Margin="8,0,0,0"
                    IsCancel="True"
                    Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
