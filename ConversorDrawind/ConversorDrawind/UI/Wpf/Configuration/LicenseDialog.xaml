<Window x:Class="ConversorDrawind.UI.Wpf.Configuration.LicenseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleActivation}"
        Width="525"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="96" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Width="230"
                   Source="pack://application:,,,/Resources/DRAWIND_PEQUENA.png" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="{x:Static local:Localization.LabelSerialNumber}"
                       VerticalAlignment="Center"
                       Margin="0,0,8,8" />
            <TextBox x:Name="SerialTextBox"
                     Grid.Row="1"
                     Grid.Column="1"
                     Margin="0,0,0,8" />

            <StackPanel Grid.Row="2"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{x:Static local:Localization.ButtonCancel}"
                        IsCancel="True"
                        Width="75"
                        Margin="0,0,6,0"
                        Click="CancelButtonClick" />
                <Button Content="{x:Static local:Localization.ButtonActivate}"
                        IsDefault="True"
                        Width="75"
                        Click="ActivateButtonClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
