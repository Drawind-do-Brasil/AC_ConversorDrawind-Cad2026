<UserControl x:Class="ConversorDrawind.UI.Wpf.TextStyles.TextStyleConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ConversorDrawind">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="{x:Static local:Localization.ButtonAddLine}" Click="AddButtonClick" />
            <Separator Visibility="Hidden" Margin="2.5"/>
            <Button Content="{x:Static local:Localization.ButtonDeleteLine}" Click="DeleteButtonClick" />
        </StackPanel>

        <DataGrid x:Name="TextStylesDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  CanUserResizeRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="TextStylesDataGridMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static local:Localization.LabelName}" Binding="{Binding Nome}" Width="1.5*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelFont}" Binding="{Binding Fonte}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelOblique}" Binding="{Binding Oblique}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelBold}" Binding="{Binding Negrito}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelSize}" Binding="{Binding Tamanho}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelWidthFactor}" Binding="{Binding WidthFactor}" Width="*" />
                <DataGridTextColumn Header="{x:Static local:Localization.LabelAngle}" Binding="{Binding Angulo}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
