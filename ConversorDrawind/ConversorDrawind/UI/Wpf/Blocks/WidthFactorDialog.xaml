<Window x:Class="ConversorDrawind.UI.Wpf.Blocks.WidthFactorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConversorDrawind"
        Title="{x:Static local:Localization.TitleWidthFactor}"
        Width="246"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="0,3,24,0"
                       Text="{x:Static local:Localization.LabelWidthFactor}" />
            <TextBox x:Name="WidthFactorTextBox"
                     Grid.Row="0"
                     Grid.Column="1"
                     Height="24"
                     PreviewTextInput="WidthFactorPreviewTextInput" />

            <StackPanel Grid.Row="1"
                        Grid.ColumnSpan="2"
                        Margin="0,24,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Button Content="{x:Static local:Localization.ButtonCancel}"
                        Width="75"
                        IsCancel="True"
                        Click="CancelClick" />
                <Button Content="{x:Static local:Localization.ButtonContinue}"
                        Width="75"
                        Margin="39,0,0,0"
                        IsDefault="True"
                        Click="ContinueClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
